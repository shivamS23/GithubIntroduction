"# Github Introduction" 
1 Create a repository in Github
2 Install GitHub cli
3 Check the version using git --version
     if good then :
4. adding ReadMe file, it is a good practice 
5. First read why version control in GitHub Important https://git-scm.com/book/en/v2/Getting-Started-About-Version-Control#ch01-getting-started
6. one of the first things you did was set up your name and email address:

$ git config --global user.name "John Doe"
$ git config --global user.email johndoe@example.com

7. https://education.github.com/git-cheat-sheet-education.pdf (Use this) -
8. Initialize using git init command
9. commit means that you will now send it to the staging enviroment using git commit -m "first commit"(COMMENT)
staging enviroment is a platform where I play before sending to the main repository
git branch -M main (changing the name from master to main branch)
git remote add origin https://github.com/shivamS23/GithubIntroduction.git (telling where you want to commit)
git push -u origin main